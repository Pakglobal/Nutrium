import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const CustomeDateTimePicker = () => {
  return (
    <View>
      <Text>CustomeDateTimePicker</Text>
    </View>
  );
};

export default CustomeDateTimePicker;

const styles = StyleSheet.create({});
