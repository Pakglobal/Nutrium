import {scale} from 'react-native-size-matters';
import Color from '../colors/Colors';

const IconStyle = {
  drawerIconSize: scale(24),
  drawerIconColor: Color.textColor,
};
export default IconStyle;

export const IconPadding ={
  padding: scale(5),
  // backgroundColor: 'red'
}
